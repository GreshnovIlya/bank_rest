# 🏁 Система управления банковскими картами (bank_rest)
Backend‑приложение на Spring Boot для управления банковскими картами: создание, просмотр, переводы, администрирование.

**🛠 Технологии и стек**: Java, Spring Boot, Spring Security + JWT, PostgreSQL, Spring Data JPA, Liquibase, Swagger UI / OpenAPI, Docker Compose

## 🧾 Функционал
Для администратора (ADMIN):
* Создание, блокировка, активация, удаление карт;
* Управление пользователями (создание, удаление);
* Просмотр всех карт в системе (с фильтрацией и пагинацией).

Для пользователя (USER)
* Просмотр своих карт (поиск + пагинация);
* Блокировка своей карты;
* Просмотр баланса своей карты;
* Переводы между своими картами.

Для неавторизованного пользователя:
* Регистрация;
* Авторизация.

## 📁 Структура проекта
* src/
* ├── main/
* │   ├── java/com/example/bankcards/       # Основной пакет приложения
* │   │   ├── controller/                   # Контроллеры API
* │   │   ├── service/                      # Бизнес‑логика
* │   │   ├── repository/                   # Репозитории JPA
* │   │   ├── model/                        # Entity‑классы
* │   │   ├── security/                     # Конфигурация безопасности
* │   │   ├── config/                       # Конфигурационные классы
* │   │   └── exception/                    # Обработчики исключений
* │   └── resources/
* │       ├── application.yml               # Конфигурация приложения
* │       ├── db/changelog/                 # Liquibase‑миграции
* ├── test/                                 # Юнит‑тесты
* └── docs/
*     └── openapi.yaml                      # Спецификация API

# 🌐 API (OpenAPI / Swagger)
Спецификация: docs/openapi.yaml
Интерфейс документации: http://localhost:8080/swagger-ui/index.html (после запуска).

# 🛰 Развёртывание (Docker Compose)
Требования:
* Docker
* Docker Compose

Запуск:
* Соберите JAR: mvn clean package;
* Запустите контейнеры: docker compose up -d.

* Сервисы:
* bank_rest — backend‑приложение (порт 8080).
* bank_rest_db — PostgreSQL (порт 5432).
